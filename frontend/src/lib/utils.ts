// Utility functions will be added here

/**
 * Example placeholder utility for future logic.
 */
export function cn(...classes: (string | undefined | null | false)[]) {
  return classes.filter(Boolean).join(' ');
}
